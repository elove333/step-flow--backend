{
  "name": "-stepflow--backend",
  "version": "1.0.0",
  "description": "Backend API for STEPFLOW, handling authentication, session ingestion, analytics, scoring, and user progress tracking.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest tests/api.test.js --forceExit --detectOpenHandles",
    "test:integration": "jest tests/folder.test.js --forceExit --detectOpenHandles --runInBand",
    "test:watch": "jest --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "mongoose": "^9.1.5"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "mongodb-memory-server": "^11.0.1",
    "nodemon": "^3.1.11",
    "supertest": "^7.2.2"
  }
}
